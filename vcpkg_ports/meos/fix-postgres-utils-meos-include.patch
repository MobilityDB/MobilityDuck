--- a/postgres/utils/CMakeLists.txt
+++ b/postgres/utils/CMakeLists.txt
@@ -10,3 +10,7 @@
 
 set_property(TARGET utils PROPERTY C_VISIBILITY_PRESET hidden)
 set_property(TARGET utils PROPERTY POSITION_INDEPENDENT_CODE ON)
+
+if(MEOS)
+  target_include_directories(utils PRIVATE "${CMAKE_SOURCE_DIR}/meos/include")
+endif()


